<div class="container m-2">
  <h3>Galaxies I have booked</h3>
  <% @bookings_as_booker.each do |booking| %>
    <div class="booking_card mb-3">
      <div class="d-flex justify-content-between align-items-center p-1">
        <h3><strong><%= link_to booking.venue.name, venue_path(booking.venue.id) %></strong></h3>
        <p>Landing date: <%= booking.date %></p>
        <p>Confirmed? <%= booking.status == "confirmed" ? "👍" : "👎" %></p>
        <%= link_to 'Cancel booking', booking_path(booking.id), class: "btn btn-primary m-2", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to cancel this booking?" } %>
      </div>
    </div>
  <% end %>
</div>
