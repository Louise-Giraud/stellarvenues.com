<div class="container">
  <h1> <%= @venue.name %> </h1>
    <%= image_tag "galaxy.jpg", class: "pic" %>
  <h3> <%= "🪐 Address: #{@venue.address}" %> </h3>
  <h4> <%= "👽 Capacity: #{@venue.capacity}" %> </h3>

  <h4><%= "👩‍🚀 Logistic partner on-site: #{@venue.logistic_partner}" %></h3>
  <h4><%= "🔦 Available ressources on-site:  #{@venue.available_ressources}" %></h3>
  <h4><%= "🛸 Access: #{@venue.access}" %></h3>

  <p><%= @venue.description %></p>

<div class="booking-form">
  <% unless current_user == @venue.user %>
    <h2><%="Book the galaxy"%></h2>
    <%= render "booking_form" %>
  <% end %>
</div>
</div>
